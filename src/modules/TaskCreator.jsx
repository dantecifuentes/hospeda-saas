import React,{useState}from'react';
export default function TaskCreator({cabins,onAdd,onClose}){const[form,setForm]=useState({cabinId:cabins[0]?.id||'',type:'Aseo',due:'',assigned:'Sin asignar'});return <div className="modalback" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="modal"><div className="modalhead"><h2>Nueva tarea operacional</h2><button onClick={onClose}>×</button></div><form className="formgrid" onSubmit={e=>{e.preventDefault();onAdd({...form,cabinId:Number(form.cabinId),due:form.due.replace('T',' '),id:Date.now(),status:'Pendiente'})}}><label>Alojamiento<select value={form.cabinId} onChange={e=>setForm(p=>({...p,cabinId:e.target.value}))}>{cabins.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label>Tipo<select value={form.type} onChange={e=>setForm(p=>({...p,type:e.target.value}))}>{['Aseo','Inspección','Mantenimiento','Preparación','Otro'].map(x=><option key={x}>{x}</option>)}</select></label><label>Vencimiento<input type="datetime-local" required value={form.due} onChange={e=>setForm(p=>({...p,due:e.target.value}))}/></label><label>Responsable<input required value={form.assigned} onChange={e=>setForm(p=>({...p,assigned:e.target.value}))}/></label><div className="formactions"><button type="button" className="soft" onClick={onClose}>Cancelar</button><button className="new">Crear tarea</button></div></form></div></div>}
